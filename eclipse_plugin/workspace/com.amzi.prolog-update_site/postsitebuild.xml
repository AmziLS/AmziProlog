<project>
	<property name="deploy" location="../../../../eclipse_IDE_plugin_update_site"/>
	
    <target name="clean">
        <delete dir="features"/>
		<delete dir="plugins"/>
		<delete file="artifacts.jar"/>
		<delete file="content.jar"/>
    </target>

    <target name="build">
        <mkdir dir="${deploy}"/>
    	<mkdir dir="${deploy}/features"/>
        <mkdir dir="${deploy}/plugins"/>
        <copy todir="${deploy}/features">
          <fileset dir="features"/>
        </copy>
        <copy todir="${deploy}/plugins">
          <fileset dir="plugins"/>
        </copy>    	
        <copy file="artifacts.jar" todir="${deploy}"/>
        <copy file="content.jar" todir="${deploy}"/>
    	<copy file="site.xml" todir="${deploy}"/>
    </target>
</project>
